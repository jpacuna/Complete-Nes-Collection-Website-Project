<h2>All Games</h2>

<%= paginate @games %>

<% @games.each do |game| %>
  <section
    <h3><%= link_to game.name, game_path(game) %></h3>
    <p><%= game.description %></p>    
  </section>
  <hr>
<% end %>
